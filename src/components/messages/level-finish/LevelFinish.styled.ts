import { WrapperV } from '@app/style';
import styled from 'styled-components';

import { Button as ButtonBase } from '@app/ui';

export const Wrapper = styled(WrapperV)`
  gap: 8px;
`;

export const Button = styled(ButtonBase)`
  margin-top: 20px;
`;
